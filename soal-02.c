/** EL2208 Praktikum Pemecahan Masalah dengan C 2020/2021
*   Modul               : 4
*   Soal                : 2
*   Hari dan Tanggal    : Senin, 15 Maret 2021
*   Nama (NIM)          : 
*   Asisten (NIM)       : 
*   Nama File           : soal-02.c
*   Deskripsi           : Deskripsi file ini.
*/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct{
    char kategori[20];
    char subkategori[20];
    int stok;
    int harga;
    int diskon;
} Barang;

void inputBarang(){
    // Fungsi untuk memasukkan data file eksternal kedalam array dinamis
    // Tuliskan implementasi Anda!
}

void hitungDiskon(){
    // Fungsi untuk menghitung diskon setiap barang sesuai stok yang ada
    // Tuliskan implementasi Anda!
}

void hargaDiskonTerendah(){
    // Fungsi menghitung harga barang sesuai diskon dan mencari harga barang termurah
    // Tuliskan implementasi Anda!
}

void cekStock(){
    // Fungsi mengecek stok barang harus di restock atau tidak
    // Tuliskan implementasi Anda!
}

int main() {
    
    // Template printf
    // printf("Masukkan file toko : ");
    // printf("\nButuh restock: %s", nama);
    // printf("Setidaknya pesan %d barang lagi untuk mencapai batas persediaan minimal!\n", jml_barang_perlu_dipesan);
    // printf("Semua persediaan barang masih banyak! Tidak perlu restock!\n");
    // printf("\nPengecekan Persedian...\n");
    // printf("\nHarga Barang Termurah: \n");
    // printf("\n");
    // printf("Kategori\t: %s", jenis);
    // printf("Sub-Kategori\t: %s", nama);
    // printf("Stok Barang\t: %d\n", stok);
    // printf("Diskon Barang\t: %d\n", diskon);
    // printf("Harga Barang\t: %d\n", harga);
    
    return 0;
}
